apiVersion: "helm.toolkit.fluxcd.io/v2beta1"
kind: "HelmRelease"
metadata:
  name: "apiserver"
  namespace: "infra"
spec:
  chart:
    spec:
      chart: "./hlm/apiserver"
      sourceRef:
        kind: "GitRepository"
        name: "apiserver"
        namespace: "infra"
  interval: "1m"
  values:
    image:
      tag: "da66ff32538dd5d06944e57cc0be0beccb319a6e"
