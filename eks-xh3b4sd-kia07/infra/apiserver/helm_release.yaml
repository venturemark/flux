apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: apiserver
  namespace: infra
spec:
  chart:
    spec:
      chart: ./hlm/apiserver
      sourceRef:
        kind: GitRepository
        name: apiserver
        namespace: infra
  install:
    remediation:
      retries: -1
  interval: 1m
  values:
    cluster:
      name: kia07
      zone: aws.venturemark.co
    image:
      tag: cfc8d4d9352d99e59d4efc526abbe668568d15d8
