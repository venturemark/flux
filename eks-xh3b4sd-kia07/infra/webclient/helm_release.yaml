apiVersion: "helm.toolkit.fluxcd.io/v2beta1"
kind: "HelmRelease"
metadata:
  name: "webclient"
  namespace: "infra"
spec:
  chart:
    spec:
      chart: "./hlm/webclient"
      sourceRef:
        kind: "GitRepository"
        name: "webclient"
        namespace: "infra"
  install:
    remediation:
      retries: -1
  interval: "1m"
  values:
    api:
      endpoint: "https://apiserver.kia06.aws.venturemark.co:443"
    cluster:
      name: "kia06"
      zone: "aws.venturemark.co"
    image:
      tag: "70b79ca6db91fdfa47a74a36e1e051ba12a1e494"
