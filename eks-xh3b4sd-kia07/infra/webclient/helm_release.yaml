apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: webclient
  namespace: infra
spec:
  chart:
    spec:
      chart: ./hlm/webclient
      sourceRef:
        kind: GitRepository
        name: webclient
        namespace: infra
  install:
    remediation:
      retries: -1
  interval: 1m
  values:
    api:
      endpoint: https://apiserver.kia07.aws.venturemark.co:443
    cluster:
      name: kia07
      zone: aws.venturemark.co
    dns:
      cname: beta.venturemark.co
    image:
      tag: d632d3b0896582062321346ca7e427bb5799829a
