apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: webclient
  namespace: infra
spec:
  chart:
    spec:
      chart: ./hlm/webclient
      sourceRef:
        kind: GitRepository
        name: webclient
        namespace: infra
  install:
    remediation:
      retries: -1
  interval: 1m
  values:
    api:
      endpoint: https://apiserver.kia07.aws.venturemark.co:443
    cluster:
      name: kia07
      zone: aws.venturemark.co
    dns:
      cname: beta.venturemark.co
    image:
      tag: 4f9c5189260364a3be9b5e02bff979c4ac80c5ef
