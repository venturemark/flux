apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: webclient
  namespace: infra
spec:
  chart:
    spec:
      chart: ./hlm/webclient
      sourceRef:
        kind: GitRepository
        name: webclient
        namespace: infra
  install:
    remediation:
      retries: -1
  interval: 1m
  values:
    api:
      endpoint: https://apiserver.kia07.aws.venturemark.co:443
    cluster:
      name: kia07
      zone: aws.venturemark.co
    dns:
      cname: beta.venturemark.co
    image:
      tag: b983f7aeac2329b19cdfa7c83103e6bd4dbf5641
