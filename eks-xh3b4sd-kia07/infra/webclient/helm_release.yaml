apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: webclient
  namespace: infra
spec:
  chart:
    spec:
      chart: ./hlm/webclient
      sourceRef:
        kind: GitRepository
        name: webclient
        namespace: infra
  install:
    remediation:
      retries: -1
  interval: 1m
  values:
    api:
      endpoint: https://apiserver.kia07.aws.breadcrumb.so:443
    cluster:
      name: kia07
      zone: aws.breadcrumb.so
    dns:
      cname: beta.breadcrumb.so
    image:
      tag: 8e4b90e15af7fbb8cc091e7931a207ab929eaab5
