apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: apiserver
  namespace: infra
spec:
  chart:
    spec:
      chart: ./hlm/apiserver
      sourceRef:
        kind: GitRepository
        name: apiserver
        namespace: infra
  install:
    remediation:
      retries: -1
  interval: 1m
  values:
    image:
      tag: 0a4df8f6ef756fcdf3de64944a6970cf4e499afa
