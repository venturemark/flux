apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: apiserver
  namespace: infra
spec:
  chart:
    spec:
      chart: ./hlm/apiserver
      sourceRef:
        kind: GitRepository
        name: apiserver
        namespace: infra
  install:
    remediation:
      retries: -1
  interval: 1m
  values:
    image:
      tag: 8bf0391025a07656633a2ea34b05cb07ab33a204
