apiVersion: "helm.toolkit.fluxcd.io/v2beta1"
kind: "HelmRelease"
metadata:
  name: "webclient"
  namespace: "infra"
spec:
  chart:
    spec:
      chart: "./hlm/webclient"
      sourceRef:
        kind: "GitRepository"
        name: "webclient"
        namespace: "infra"
  install:
    remediation:
      retries: -1
  interval: "1m"
  values:
    api:
      endpoint: "http://127.0.0.1:7777"
    image:
      tag: "9f2d77196031779ccd3ce212490868a2ed2bd4e7"
