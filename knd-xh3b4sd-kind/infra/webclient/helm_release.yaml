apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: webclient
  namespace: infra
spec:
  chart:
    spec:
      chart: ./hlm/webclient
      sourceRef:
        kind: GitRepository
        name: webclient
        namespace: infra
  install:
    remediation:
      retries: -1
  interval: 1m
  values:
    api:
      endpoint: http://127.0.0.1:7777
    image:
      tag: afbc8f95b0c36d0e34eb467c836524bf3b7f1d5a
