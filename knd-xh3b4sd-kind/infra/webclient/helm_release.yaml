apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: webclient
  namespace: infra
spec:
  chart:
    spec:
      chart: ./hlm/webclient
      sourceRef:
        kind: GitRepository
        name: webclient
        namespace: infra
  install:
    remediation:
      retries: -1
  interval: 1m
  values:
    api:
      endpoint: http://127.0.0.1:7777
    image:
      tag: b3d44e6ca40de40dc8ed141f042215a7c937d76d
