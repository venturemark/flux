apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: apiserver
  namespace: infra
spec:
  chart:
    spec:
      chart: ./hlm/apiserver
      sourceRef:
        kind: GitRepository
        name: apiserver
        namespace: infra
  install:
    remediation:
      retries: -1
  interval: 1m
  values:
    image:
      tag: 256ea33c31cd9430af303ff8486b49d891dec572
