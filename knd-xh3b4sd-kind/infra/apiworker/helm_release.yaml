apiVersion: "helm.toolkit.fluxcd.io/v2beta1"
kind: "HelmRelease"
metadata:
  name: "apiworker"
  namespace: "infra"
spec:
  chart:
    spec:
      chart: "./hlm/apiworker"
      sourceRef:
        kind: "GitRepository"
        name: "apiworker"
        namespace: "infra"
  install:
    remediation:
      retries: -1
  interval: "1m"
  values:
    image:
      tag: "ddb414b05608799153c822e818da3b7d5d444bd0"
